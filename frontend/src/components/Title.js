import React from "react";


const Title = () => {

    return(
        <div className="title">
            <h1 className="text">Cadastro de usuários</h1>
        </div>

    );
};

export default Title;